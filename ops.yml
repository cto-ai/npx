# for more info visit https://cto.ai/docs/ops-reference
version: "1"
commands:
  - name: npx:0.1.0
    public: true
    description: Run npx securely from within a containerized workflow
    run: npx
    sourceCodeURL: "https://github.com/cto-ai/npx"
    help:
      usage: "ops run npx <pkg>"
      arguments:
        pkg: "<pkg>"
      options:
        help: "-h"
